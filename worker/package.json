{
  "name": "onecontract-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute onecontract --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute onecontract --file=schema.sql --remote",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "cors": "^2.8.6",
    "docxtemplater": "^3.50.0",
    "express": "^5.2.1",
    "hono": "^4.6.0",
    "multer": "^2.1.1",
    "pizzip": "^3.1.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.88.0"
  }
}
